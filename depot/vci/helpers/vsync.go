package helpers

// copy one folder to one azure share.

type VSync struct {
}

func (v *VSync) CopyFolderToAzureShare() error {

	// mounter := mount.NewMounter()
	// Mount(source string, target string, fstype string, options []string)

	// tmpl := template.Must(template.New("").Parse(stagerScript))
	// if err := tmpl.Execute(scriptBuf, struct {
	// 	RSync         bool
	// 	BuildpackMD5s []string
	// }{
	// mounter.Mount()

	return nil
}
